/**
 * The artboards define no focus-visible styling at all (design.md §8.5).
 * With radius 0 and no shadows, a plain 2px outline at 2px offset is the one
 * treatment that stays visible on every surface in this palette.
 */
export const FOCUS_RING =
  "outline-none focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-haba-green";
